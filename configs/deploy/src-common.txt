# Paths under repository root shared by all deployment targets.
# One path per line. Blank lines and lines starting with # are ignored.
src/ros/unitree_ros2
src/ros/cmd_vel_control
src/zenoh
src/zenoh-plugin-ros2dds
